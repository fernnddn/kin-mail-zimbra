---
# iSCSI SBD target on Monitoring VM (task 2.1 §3).
# Dry-run only against live lab:
#   ansible-playbook playbooks/mon-iscsi-target.yml --check

- name: Siapkan target iSCSI SBD di VM Monitoring
  hosts: monitoring
  gather_facts: true
  become: true
  roles:
    - role: iscsi_target
      tags: [iscsi, target, monitoring]
