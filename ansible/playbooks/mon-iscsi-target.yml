---
# iSCSI SBD target on Monitoring VM (task 2.1 §3).
# Dry-run only against production cluster:
#   ansible-playbook playbooks/mon-iscsi-target.yml --check

- name: Set up the SBD iSCSI target on the Monitoring VM
  hosts: monitoring
  gather_facts: true
  become: true
  roles:
    - role: observability_identity
      tags: [observability, identity]
    - role: iscsi_target
      tags: [iscsi, target, monitoring]
