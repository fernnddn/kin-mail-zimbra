---
- name: Install the Pacemaker, Corosync, and pcs packages on the new node
  ansible.builtin.apt:
    name: "{{ cluster_node_base_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  tags: [cluster, base, packages]
