---
- name: Menginstal paket SBD dan fence_sbd
  ansible.builtin.apt:
    name: "{{ sbd_stonith_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  tags: [sbd, stonith, install]
