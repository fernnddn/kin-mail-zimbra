---
- name: Install the SBD and fence_sbd packages
  ansible.builtin.apt:
    name: "{{ sbd_stonith_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  tags: [sbd, stonith, install]
