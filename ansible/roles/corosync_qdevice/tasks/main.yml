---
# Entry — granular includes; labels are progress-UI ready.
# Source: Phase 2 task 2.2 §1 (qdevice client wiring only).

- name: Include the qdevice package installation steps
  ansible.builtin.import_tasks: install.yml
  tags: [qdevice, install]

- name: Include the qdevice TLS certificate steps
  ansible.builtin.import_tasks: tls.yml
  tags: [qdevice, tls]

- name: Include the quorum device configuration steps
  ansible.builtin.import_tasks: configure.yml
  tags: [qdevice, configure]

- name: Include the qdevice service activation steps
  ansible.builtin.import_tasks: service.yml
  tags: [qdevice, service]

- name: Include the qdevice verification steps
  ansible.builtin.import_tasks: verify.yml
  tags: [qdevice, verify]
