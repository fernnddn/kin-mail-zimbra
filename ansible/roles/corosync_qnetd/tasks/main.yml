---
# Entry point - one include per progress-friendly step.
# Mapped from Phase 2 task 2.1 §2 (qnetd on Monitoring only).

- name: Include the qnetd package installation steps
  ansible.builtin.import_tasks: install.yml
  tags: [qnetd, install]

- name: Include the qnetd TLS certificate steps
  ansible.builtin.import_tasks: tls.yml
  tags: [qnetd, tls]

- name: Include the qnetd service activation steps
  ansible.builtin.import_tasks: service.yml
  tags: [qnetd, service]

- name: Include the qnetd verification steps
  ansible.builtin.import_tasks: verify.yml
  tags: [qnetd, verify]
