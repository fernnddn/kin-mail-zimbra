---
# Entry point — one include per progress-friendly step (Indonesian labels).
# Mapped from docs/progress/2.1-monitoring-vm-qnetd-sbd-target.md §2 only.

- name: Sertakan langkah instalasi paket qnetd
  ansible.builtin.import_tasks: install.yml
  tags: [qnetd, install]

- name: Sertakan langkah sertifikat TLS qnetd
  ansible.builtin.import_tasks: tls.yml
  tags: [qnetd, tls]

- name: Sertakan langkah aktivasi layanan qnetd
  ansible.builtin.import_tasks: service.yml
  tags: [qnetd, service]

- name: Sertakan langkah verifikasi qnetd
  ansible.builtin.import_tasks: verify.yml
  tags: [qnetd, verify]
