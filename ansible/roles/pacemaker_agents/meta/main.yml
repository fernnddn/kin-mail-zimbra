---
galaxy_info:
  role_name: pacemaker_agents
  description: >-
    resource-agents-extra, ocf:kin:zimbra, dual-primary assert helper, disable
    systemd drbd.service (Pacemaker mail-stack stage preconditions + Stage C OCF).
  license: proprietary
  min_ansible_version: "2.14"
dependencies:
  - role: kin_mail_paths
