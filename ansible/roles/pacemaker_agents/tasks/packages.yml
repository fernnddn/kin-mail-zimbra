---
- name: Install the resource-agents-extra package for Filesystem and IPaddr2
  ansible.builtin.apt:
    name: "{{ pacemaker_agents_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  tags: [pacemaker, agents, packages]
