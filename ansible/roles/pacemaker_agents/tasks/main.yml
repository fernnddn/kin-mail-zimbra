---
- name: Include the resource-agents package installation steps
  ansible.builtin.import_tasks: packages.yml
  tags: [pacemaker, agents, packages]

- name: Include the Zimbra OCF deployment steps
  ansible.builtin.import_tasks: ocf.yml
  tags: [pacemaker, agents, ocf]

- name: Include the dual-primary assert script steps
  ansible.builtin.import_tasks: assert.yml
  tags: [pacemaker, agents, assert]

- name: Include the DRBD systemd adjustment steps
  ansible.builtin.import_tasks: systemd.yml
  tags: [pacemaker, agents, systemd]

- name: Include the Pacemaker agent verification steps
  ansible.builtin.import_tasks: verify.yml
  tags: [pacemaker, agents, verify]
