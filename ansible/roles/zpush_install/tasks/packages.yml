---
- name: Install PHP-FPM and Z-Push helper packages
  ansible.builtin.apt:
    name: "{{ zpush_php_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  tags: [zpush, packages]
