---
- name: Install fail2ban and unattended-upgrades
  ansible.builtin.apt:
    name: "{{ os_hardening_packages }}"
    state: present
    update_cache: true
    cache_valid_time: 3600
  tags: [hardening, os, packages]
