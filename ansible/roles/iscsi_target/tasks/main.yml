---
- name: Include the iSCSI target package installation steps
  ansible.builtin.import_tasks: install.yml
  tags: [iscsi, target, install]

- name: Include the SBD LUN provisioning steps
  ansible.builtin.import_tasks: lun.yml
  tags: [iscsi, target, lun]

- name: Include the targetcli configuration steps
  ansible.builtin.import_tasks: targetcli.yml
  tags: [iscsi, target, targetcli]

- name: Include the target service activation steps
  ansible.builtin.import_tasks: service.yml
  tags: [iscsi, target, service]

- name: Include the iSCSI target verification steps
  ansible.builtin.import_tasks: verify.yml
  tags: [iscsi, target, verify]
