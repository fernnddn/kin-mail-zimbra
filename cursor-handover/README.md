# Material for Cursor

Parked, not active. As of 11 September 2026 the work is being done here, in
this repository, by Claude. Nothing in this folder is a live instruction to
anyone.

It exists so that when the operator does decide to move a long technical task
to Cursor, the context is already written down rather than reconstructed from
memory at the point of handover.

| File | What it is |
| --- | --- |
| `00-HANDOVER-0.1.10.md` | State of the mail gateway work: what is done, what is not, what to confirm against a real PMG first, and the house rules of this repository. |

**Before handing any of this to Cursor, re-read it against the code.** It is a
snapshot, and snapshots go stale — the 0.1.10 work has already moved on from
the first version of that document more than once in a single evening.

The design itself is not in here. It lives at the repository root, where it
belongs, and it is the product's documentation rather than anyone's handover
notes:

- `MAIL-GATEWAY.md` — architecture, ports, DNS, every PMG setting and why
- `DEPLOY-WITH-GATEWAY.md` — the step-by-step deployment runbook
