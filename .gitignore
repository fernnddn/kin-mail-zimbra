# Working documents and progress logs stay local (not production code)
docs/

# Local Cursor / Claude tooling
.claude/

# Ansible local inventory / secrets (keep example committed)
ansible/inventory/lab.yml
ansible/inventory/add-host.yml
ansible/inventory/*.local.yml
ansible/**/*.retry
*.retry

# --- never commit deployment state or credentials ---------------------------
config
*.conf.local
cloudflare.ini
*.pem
*.key
*.crt
*.p12
*.pfx
**/*ed25519-private*
**/.kin-mail-license/

# --- installer artefacts -----------------------------------------------------
*.tgz
*.tar.gz
*.sha256
*.md5
zcs-*/

# --- admin console build deps (dist is committed for appliance bootstrap) ----
console/frontend/node_modules/
console/backend/.venv/
console/**/__pycache__/
console/**/*.pyc
**/__pycache__/
*.pyc


# --- logs and scratch --------------------------------------------------------
*.log
*.bak
*.bak.*
/tmp/
cap.txt

# --- internal documents: these must never leave the company ------------------
*.docx
*.pdf

# KIN license issue ledger (local to the generator machine, not the product)
licensing-generator/license-ledger.csv
licensing-generator/license-ledger.jsonl

# --- editors and OS ----------------------------------------------------------
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db
